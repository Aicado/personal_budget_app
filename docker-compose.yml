version: '3.8'

services:
  ynab-analyzer:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./data:/app/data:ro
      - ./db:/app/db
    command: uv run -m uvicorn src.backend.main:app --host 0.0.0.0 --port 8000
